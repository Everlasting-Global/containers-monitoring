global:
  scrape_interval: 30s  # Set the scrape interval to every 30 seconds.
  evaluation_interval: 30s  # Evaluate rules every 30 seconds.

scrape_configs:
  - job_name: "prometheus"

storage:
  tsdb: 
    out_of_order_time_window: 5s
